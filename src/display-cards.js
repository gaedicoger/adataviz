//Gestion de l'affichage des cartes
//A intégrer dans une fonction
const cardTree = `
        <img src="${img}" class="pokemon-card-image" />
        <h2 class="tree-card-title">${name}</h2>
        <p class="tree-card-species">${typesString}</p>
        <p class="tree-card--under-species">${typesString}</p>
        <p class="tree-card-year_plantation">${typesString}</p>
        <p class="tree-card-age">${number}</p>
        <p class="tree-card-height">${number}</p>
        <p class="tree-card-grow-stade">${img}</p>`;
//Gestion du bouton "See more" pour afficher la description
